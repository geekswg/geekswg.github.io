# Cloudflare部署CFnew终端


[CFnew](https://github.com/byJoey/cfnew) - 终端 v2.9

## 介绍

- 🎭 多协议支持 - VLESS + Trojan + xhttp，自由切换
- 🛤️ 自定义路径 - 告别UUID路径，自定义访问地址，支持多级路径
- ⚡ 延迟测试 - 内置IP延迟测试，自动获取机场码，多线程并发测试
- 🔄 订阅转换 - 自定义转换服务，优选类型细粒度控制
- 🎯 图形化管理 - KV存储，实时配置，无需重新部署
- 🚀 API管理 - RESTful API，批量操作，动态更新
- 📱 多客户端支持 - CLASH、SURGE、SING-BOX、LOON、QUANTUMULT X、- V2RAY、Shadowrocket、STASH、NEKORAY、V2RAYNG
- 🚀 应用唤醒 - 点击按钮自动唤醒对应客户端应用
- 🔍 自动识别 - 根据User-Agent自动识别并返回对应格式
- 🌐 多语言支持 - 支持中文和波斯语（伊朗语），根据浏览器语言自动切换

## 部署

利用Cloudflare Pages部署CFnew终端，无需服务器，无需域名，无需配置，即可在Cloudflare Pages上部署CFnew终端。

1. 下载CFnew终端部署zip包- 从[CFnew GitHub仓库](https://github.com/byJoey/cfnew/releases)下载最新版本Pages.zip。
2. 登录[Cloudflare账户](https://dash.cloudflare.com/)，点击右上角加号选择`页面`，创建一个新的Pages项目,选择`拖放文件`开始使用，输入项目名称`cf-new`、创建项目,把刚刚下载的Pages.zip拖放到页面上，点击`部署`。
3. 点击刚刚创建的Pages项目，进入`设置`，在`变量和机密`添加文本变量，`u`,值为你随机生成一个uuid，可以在[UUID在线生成器](https://www.lddgo.net/string/uuid)生成，复制生成的uuid，粘贴到`u`的值中。
4. 在`存储和数据`菜单中，点击`Workers KV`，右上角创建一个KV实列，`Create Instance`，实例名称`CFNEW_KV`，回到刚刚创建的Pages项目点击`设置`,`绑定`，添加绑定，类型选择`Workers KV命名空间`，名称`C`，命名空间选择刚刚创建的实例，保存。
5. 点击`部署`,右上角`创建部署`,创建一个新的部署，等待部署完成，点击`查看部署`，访问你的CFnew终端页面了。需要注意的是，cf自己提供的域名国内访问不稳定，经常打不开，建议绑定自己的域名使用。

> [!NOTE]
> 刚刚生成的UUID请妥善保管，用于后续访问时输入，如果忘记可以去Cloudflare Pages项目设置中查看，或者重新新生成一个配置，重新修改配置后需要重新部署才能生效。

---

> 作者: [geekswg](https://github.com/geekswg)  
> URL: https://geekswg.js.cool/posts/2026/cf-new/  

